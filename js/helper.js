// function computerChoice(n, length) {
	// 	for(let i = 0; i < length; i++) {
	// 		if(computerNums.length < length) {
	// 			computerNums.push(Math.floor(Math.random() * n + 1));
	// 		}
	// 	}
	// 	return computerNums;
	// }
	
	// console.log(computerChoice(36, 5));
	
	// return computerNums = [...arr].sort(() => 0.5 - Math.random()).slice(0, length);
		// const gameArr = new Array(36).fill().map((val, i) => 1 + i).sort(() => 0.5 - Math.random()).slice(0, length);
	
	
		// return computerNums.slice(0, length);
	
		// arr.forEach(() => {
		// 	if(computerNums.length < length) {
		// 		computerNums.push(arr[Math.floor(Math.random() * arr.length)]);
		// 	}
		// })
	
	
		// console.log(getCompRandoms(thirtySix, 5));
	
	
	
		// ПОЛУЧИТЬ МНОГО РАНДОМНЫХ ЭЛЕМЕНТОВ  В МАССИВЕ
	// function getMultipleRandom(n, len) {
	// 	const gameArr = new Array(n).fill().map((_, i) => i + 1); //Create 36 numbers
	// 	const numCount = len;
	
	// 	while(computerNums.length < numCount) {
	// 		const randomIdx = Math.floor(Math.random() * gameArr.length);
	// 		const randomN = gameArr[randomIdx];
	
	// 		if(!computerNums.includes(randomN)) {
	// 			computerNums.push(randomN)
	// 		}
	// 	}
	// 	console.log(computerNums);
	// 	return computerNums;
	// }